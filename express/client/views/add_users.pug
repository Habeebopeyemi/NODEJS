extends layouts/main-layout.pug

block content
    main
        form.product-form(action="/admin/users", method="POST")
            .form-control
                label(for="title")
                input(type="text", name="title")#title
            button(type="submit") Add User